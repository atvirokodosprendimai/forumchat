# binaries
/bin/
/dist/
/forumchat
/app

# go build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# generated code — *_templ.go ARE committed so `git pull` + `go build` deploys
# pick up template changes without running `templ generate` on the host.
# Regenerate with `make gen`; never hand-edit. Only the auxiliary text tables
# (not needed to build) stay ignored.
*_templ.txt

# data
/data/
*.db
*.db-*
*.sqlite
*.sqlite-*
*.sqlite3

# mempalace / chroma local tooling state (not project files)
/chroma.sqlite3
/entities.json
/mempalace.yaml

# uploads
/uploads/

# tmp
/tmp/
out.log
*.log

# env
.env
.env.local
.env.*.local
!.env.example

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp
*.bak
